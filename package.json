{
  "name": "smart-industrial-asset-intelligence",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix app-technicien && npm install --prefix web-manager-admin",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix app-technicien\" \"npm run dev --prefix web-manager-admin\"",
    "dev:server": "npm run dev --prefix server",
    "dev:technicien": "npm run dev --prefix app-technicien",
    "dev:web": "npm run dev --prefix web-manager-admin",
    "seed": "node server/db/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
